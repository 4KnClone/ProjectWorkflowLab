{{#each books as |book|}}

<div data-id="{{book.id}}">
  <h3>Title: {{book.title}}</h3>
  <p>
    {{#if book.author}}
        Author: {{book.author}}
      {{else}}
        unknown author.
      {{/if}}
  </p>
<hr>
</div>
{{/each}}
